# Secrets — never commit
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Local databases / run artifacts
*.db

# Session-local sim toolchain (micromamba env, prototypes) — never commit (multi-GB)
.tooling/

# Scoring-validation intermediates (receptor PDBs, PDBQT, docked poses); the
# self-contained results/ (scores.json + figure) are committed instead.
trial-impact-service/validation/work/
trial-impact-service/validation/congeneric/work/
trial-impact-service/validation/pose_fidelity/work/

# Session-local scratch — experiment probes, seed/serve harnesses, one-off design
# notes and test plans/reports that are not part of the published project.
_*.py
trial-impact-service/_*.json
DESIGN_NOTE_*.md
TEST_PLAN_*.md
test-report.md

# OS / editor
.DS_Store
